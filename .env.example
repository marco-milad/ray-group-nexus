# Used by `vite dev` (npm run dev). Loaded into process.env via vite.config.ts.
# Production secrets (Cloudflare Workers) are set via `wrangler secret put`.
RESEND_API_KEY=re_your_resend_api_key_here
