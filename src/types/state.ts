/**
 * UI state machine — re-exported from lib/stateUtils for the canonical
 * `@/types/state` import path used across orchestration code.
 */
export type { UIState } from "@/lib/stateUtils";
export { STATE_PRIORITY, resolveState } from "@/lib/stateUtils";
